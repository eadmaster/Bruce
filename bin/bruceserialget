#!/bin/bash

# args checking
if [ -z "$1" ] || [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
	echo "usage: $(basename $0) INPUT_FILE"
	echo "default getting from sdcard if present"
	exit 0
fi


echo "stoarage read $@" | busybox microcom -s 115200 /dev/ttyACM0 -t 1000